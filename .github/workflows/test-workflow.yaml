name: test-workflow

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
    - name: Get dependencies
      run: |
        if [ -z "${{ secrets.HOGE }}" ]; then
        echo "empty"
        fi
        echo "Hello from folked repository ${{ secrets.HOGE }}"
        